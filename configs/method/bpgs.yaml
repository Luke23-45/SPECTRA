# @package _global_
method_name: bpgs
use_alb: false

method:
  name: bpgs
  temperature: 2.0 # Softmax temperature for stateless scale equalization

module:
  engine:
    _target_: spectra.engine.optimizers.BPGSEngine

train:
  grad_clip: 1.0
  precision: 32
