# @package _global_
method_name: bpgs
use_alb: false

method:
  name: bpgs
  omega_min: 0.1   # Minimum weight limit
  omega_max: 10.0  # Maximum weight limit
  omega_init: 1.0  # Starting weight

module:
  engine:
    _target_: spectra.engine.optimizers.BPGSEngine

train:
  grad_clip: 1.0
  precision: 32
