# @package _global_
method_name: bpgs
use_alb: false

method:
  name: bpgs
  temperature: 6.0
  auto_calibrate: true
  theta_grad_scale: 100.0
  kappa: 0.1

module:
  engine:
    _target_: spectra.engine.optimizers.BPGSEngine

train:
  grad_clip: 1.0
  precision: 32
