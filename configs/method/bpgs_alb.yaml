# ─── B-PGS + ALB (Ours — Full SPECTRA) ───────────────────────
name: bpgs_alb

# B-PGS parameters
use_sigmoid: true
use_autocal: true
s_min: -2.0
s_max: 10.0
ema_decay: 0.99

# ALB parameters
use_alb: true
n_expert_layers: 3
expert_init: orthogonal
expert_gain: 1.2
