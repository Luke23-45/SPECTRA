# @package _global_
# Archived experimental composite. Not part of the primary paper-track benchmark set.
method_name: bpgs_alb
use_alb: true

method:
  name: bpgs_alb
  s_min: -10.0
  s_max: 10.0
  s_init: 0.0
  n_expert_layers: 3
  expert_init: orthogonal
  expert_gain: 1.2

module:
  engine:
    _target_: spectra.engine.optimizers.BPGSEngine

train:
  grad_clip: 1.0
  precision: 32
